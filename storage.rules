rules_version = '2';
service firebase.storage { match /b/{bucket}/o { function signedIn(){return request.auth != null;} match /users/{userId}/{allPaths=**}{allow read,write:if signedIn() && request.auth.uid==userId;} match /conversations/{conversationId}/{allPaths=**}{allow read,write:if signedIn();} match /advisor_documents/{advisorId}/{allPaths=**}{allow write:if signedIn()&&request.auth.uid==advisorId;allow read:if signedIn();} } }
